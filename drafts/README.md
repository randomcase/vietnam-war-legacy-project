# drafts/

Writing iterations. Word and PDF files are ignored by git on purpose; keep drafts as Markdown if they are to be versioned.

Nothing here is a source. A sentence in a draft that carries a number must point back to `sources/` or `data_and_tech/`, or it is a claim.
